interface:
  display_name: "Maintain Verification Skill"
  short_description: "Keep a verify skill and feature map honest"
  default_prompt: "Use $maintain-verification-skill to audit this project's verify skill and feature map against the real app."
policy:
  allow_implicit_invocation: false

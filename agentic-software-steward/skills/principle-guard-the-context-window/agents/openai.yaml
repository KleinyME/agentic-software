interface:
  display_name: "Principle: Guard The Context Window"
  short_description: "Bulk to fresh contexts, summaries in the thread"
  default_prompt: "Use $principle-guard-the-context-window to push this bulk work into a fresh context and return only the findings."
policy:
  allow_implicit_invocation: false

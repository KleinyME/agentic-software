interface:
  display_name: "Principle: Prove It Works"
  short_description: "Verify the real artifact, not a proxy"
  default_prompt: "Use $principle-prove-it-works to verify this against the real artifact before I call it done."
policy:
  allow_implicit_invocation: false

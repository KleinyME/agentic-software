interface:
  display_name: "Grill Me"
  short_description: "Start a relentless plan interview"
  default_prompt: "Use $grill-me to stress-test this idea before I commit to it."
policy:
  allow_implicit_invocation: false

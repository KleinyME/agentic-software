interface:
  display_name: "Create Verification Skill"
  short_description: "Generate a project-local verify skill"
  default_prompt: "Use $create-verification-skill to generate a project-local verify skill and feature map for this app."
policy:
  allow_implicit_invocation: false
